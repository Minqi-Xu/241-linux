int isInteger(int id) { return 1; }
int isPointer(int* id) { return 1; }
int wain(int* a, int b) {
	int c = 0;
	int d = 0;
	d = isPointer(a+b);
	if(isInteger(b+b) >= 1) {
		println(isPointer(b+a));
	} else {
		println(isPointer(a+b));
	}
	return 0;
}
